
def distance(a, b):
    return abs(a[0] - b[0]) + abs(a[1] - b[1])

def get_result():
    car_pos = []
    res = []
    res.append()
    for vehicule in range(0, ):
        step = 0
        for ride in rides:
            if ride is not None:

